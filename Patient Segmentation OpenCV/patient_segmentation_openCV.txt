class patient_segmentation:
	str data_path;
	get_data():
		df = pd.read_csv(data_path)
		train_image,test_image,train_label, test_label = train_test_split(df,split_ratio=0.20)
		return (train_image,test_image,train_label, test_label)

	data_normalization(data):
		train_image = data[0]
		test_image = data[1]
		train_image = train_image.reshape(28,28)
		test_image = test_image.reshape(28,28)
		return (train_image,test_image,data[2],data[3])

	model_initialization():
		model = Sequential([
  			layers.Rescaling(1./255, input_shape=(img_height, img_width, 3)),
  			layers.Conv2D(16, 3, padding='same', activation='relu'),
  			layers.MaxPooling2D())
		return model
	
	model_compilation():
		model.compile(optimizer,loss,metrics)
	
	model_train():
		history = model.fit((train_image,train_label),epochs=10)

	model_prediction():
		predictions = model.predict(test_image)

int main():
	patient_segmentation disease
	disease.data_path = "path/to/disease/data"

	patient_segmentation gender
	gender.data_path = "path/to/gender/data"

	patient_segmentation ethnicity
	ethnicity.data_path = "path/to/ethnicity/data"
